{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Rust Test Coverage",
            "type": "shell",
            "command": "cargo tarpaulin --out lcov",
            "problemMatcher": [],
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}
